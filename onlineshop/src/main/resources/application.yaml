spring:
  datasource:
    url: jdbc:mysql://localhost:3306/shop_db?serverTimezone=Europe/Riga
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: system
  jpa:
    hibernate:
      show-sql: true
      ddl-auto: validate